#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>

using namespace std;

int main(){

    /// Example 1 Equality test

///    string answer;
/*    char answer;

    cout << "What is your answer Y/N ? : ";
    cin >> answer;

    if(answer == 'Y' || answer == 'y')
//    if(answer != "N")
        cout << "This is a Yes";
    else
        cout << "This is a no";
*/

    /// Example 2 Equality test for doubles
/*
    double root, number;

    root =sqrt(3);
    number = pow(root,2);

    if(number == 3)
        cout << "sqrt(3) * sqrt(3) = 3" << endl;
    else
        cout << "What just happened?" << endl;
*/
    /// Example 3 More on Equality test for doubles
/*
    double root, number;

    root =sqrt(3);
    number = pow(root,2);

    cout << setprecision(18) << "Number is : " << number << endl;
    double precision = 1E-14;

    if(abs(number - 3) < precision)
        cout << "sqrt(3) * sqrt(3) = 3" << endl;
    else
        cout << "What just happened?" << endl;

*/

    /// Example 4 Strings comparison (alphabetical order)

/*
    string name1, name2;

    cin >> name1 >> name2;

    if(name1 < name2)
        cout << name1 << endl << name2 << endl;
    else
        cout << name2 << endl << name1 << endl;

    cout << name2 + "1" << endl;
*/
    /// Example 5 Avoid duplications

    int num;
    cin >> num;

    if(num < 20 )
        num  *= 2;


    else
        num /= 2;



    cout << num << endl;

}
